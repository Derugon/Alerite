Affichage de la map => client

lors d'un déplacement 

	-> joueur se déplace si la fonction est autorisé (collision)
		-> toutes les X temps, envoie des données au serveur qui renvoie la nouvel position a tout les client
		
lors de l'affichage des joueurs

	-> serveur : envoie la position des autres joueurs au client
		client : affiche le joueur
			données envoyés : apparence et position
			
affichage du décor

	-> affichage de la map sur le client
	-> map : ne bouge pas
	-> a chaque nouvel génération : envoie du serveur au client des données à généré
			-> modèles fixes : herbe, maison ....
			-> modèles mobile : bloc de minerai, arbre, buisson ...
